{
  "name": "@octorelease/npm",
  "version": "0.0.1",
  "description": "Octorelease plugin to perform actions related to NPM.",
  "main": "lib/index.js",
  "files": [
    "lib",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc -b && npm run lint",
    "lint": "eslint src",
    "pack": "esbuild src/index.ts --bundle --outfile=../../dist/npm.js --platform=node",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "cd ../.. && npm install --package-lock-only --ignore-scripts --no-audit",
    "watch": "tsc -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@actions/exec": "^1.1.0",
    "find-up": "^5.0.0"
  },
  "peerDependencies": {
    "@octorelease/core": "^0.1.0"
  }
}
