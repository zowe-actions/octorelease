{
  "name": "@octorelease/git",
  "version": "0.0.1",
  "description": "Octorelease plugin to perform actions on a Git repository.",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint src/**/*.ts",
    "pack": "esbuild src/index.ts --bundle --outfile=../../dist/git.js --platform=node",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/exec": "^1.1.0",
    "@octorelease/core": "^0.0.1"
  }
}
